<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reviews de Receta</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #333;
        }
        .review-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
        .review-item {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        .review-item h2, .review-item p {
            margin: 0;
            padding: 0;
        }
        .review-item a {
            display: block;
            padding: 10px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            text-align: center;
            margin-top: 10px;
        }
        .review-item a:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Reviews de Receta</h1>
        <ul class="review-list">
            <% reviews.forEach(review => { %>
                <li class="review-item">
                    <p><h2>Usuario: </h2> <%= review.user %></p>
                    <p><h2>Calificación: </h2> <%= review.Rating %></p>
                    <p><h2>Comentario: </h2> <%= review.Review %></p>
                    <!-- Agrega más campos según sea necesario -->
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
